<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="assets/style/reset.css" rel="stylesheet" type="text/css" />
    <link href="assets/style/header.css" rel="stylesheet" type="text/css" />
    <link href="assets/style/event.css" rel="stylesheet" type="text/css" />
    <title>Document</title>
</head>
<body>
  {% include "header.html.twig"%}




    <h3>Evenements</h3>
    <section id="articles">
    {% for event in events %}
        <article>
            <img src="http://localhost/images/{{event.image}}" alt="{{event.image}}">
            <div>
                <h6>{{ event.description }}</h6>
                <div>
                    <h6>{{event.date}}</h6>
                    <h6>{{event.place}}</h6>
                </div>
                <a href="">En savoir plus</a>
            </div>
        </article>
    {% endfor %} 
    </section>

    {% block content %}{% endblock %}

</body>
</html>